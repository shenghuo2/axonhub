type TierCost {
  upTo: Int
  units: Int!
  subtotal: Decimal!
}

type CostItem {
  itemCode: PriceItemCode!
  quantity: Int!
  tierBreakdown: [TierCost!]
  baseSubtotal: Decimal
  priceMultiplier: Decimal
  subtotal: Decimal!
}

input TierCostInput {
  upTo: Int
  units: Int!
  subtotal: Decimal!
}

input CostItemInput {
  itemCode: PriceItemCode!
  quantity: Int!
  tierBreakdown: [TierCostInput!]
  baseSubtotal: Decimal
  priceMultiplier: Decimal
  subtotal: Decimal!
}
